import React from 'react';
import styled from '@emotion/styled';

const StyledLeftbar = styled.div({
  border: '1px solid blue',
});

const Leftbar = () => {
  return (
    <StyledLeftbar>leftbar</StyledLeftbar>
  );
};

export default Leftbar;
